# NLPfusion

A project aimed at integrating multiple natural language processing models and utilities.

## Overview
NLPfusion is designed to leverage various NLP models to create a unified application that can perform tasks such as text classification, named entity recognition, and more.

## Installation
To install the required packages, use the following command:

```bash
pip install -r requirements.txt
```
